import { Box, Container } from '@chakra-ui/react'
import React from 'react'

function News() {
  return (
    <>
      <Box display="flex">
        <Container width={40} height={300}>
          test data, cards with news?
        </Container>
        <Container width={40} height={300}>
          test data, cards with news?
        </Container>
        <Container width={40} height={300}>
          test data, cards with news?
        </Container>
        <Container width={40} height={300}>
          test data, cards with news?
        </Container>
        <Container width={40} height={300}>
          test data, cards with news?
        </Container>
        <Container width={40} height={300}>
          test data, cards with news?
        </Container>
      </Box>
    </>
  )
}

export default News
